% !TeX root = main.tex
\section{Thực nghiệm minh họa}
\label{SEC_thuc_nghiem}

\subsection{Mục đích của phần thực nghiệm}
\label{SUB_SEC_muc_dich_thuc_nghiem}
Phần này trình bày các thực nghiệm minh họa nhỏ nhằm kiểm tra trực giác của các phương pháp đã học và quan sát sự khác biệt giữa chúng trong một số bài toán đơn giản.
\medskip

Trọng tâm của phần thực nghiệm không phải là xây dựng một hệ thống phần mềm lớn, mà là quan sát các hiện tượng sau:
\begin{itemize}
    \item Gauss-Newton hội tụ nhanh khi khởi tạo tốt, còn LM ổn định hơn khi khởi tạo chưa tốt.
    \item IRLS cho hồi quy $L^1$ ít bị kéo lệch bởi ngoại lai hơn bình phương tối thiểu.
    \item K-means giảm hàm mục tiêu sau mỗi vòng lặp và tách được các cụm dữ liệu mô phỏng.
    \item ADMM tách một bài toán có ràng buộc thành các bước cập nhật đơn giản.
\end{itemize}
\medskip

Các thí nghiệm được viết bằng Python, NumPy và Matplotlib, không dùng thư viện tối ưu chuyên dụng. Điều này phù hợp với mục tiêu của báo cáo: hiểu cấu trúc thuật toán trước khi dùng các bộ giải có sẵn.
\medskip

Phần thực nghiệm gồm các nhóm minh họa chính:
\begin{itemize}
    \item khớp đường cong và biểu đồ hội tụ của Gauss-Newton/LM;
    \item hồi quy $L^1$ bằng IRLS khi dữ liệu có ngoại lai;
    \item quỹ đạo hạ theo tọa độ hai chiều;
    \item kết quả phân cụm và hàm mục tiêu của k-means;
    \item hội tụ của ADMM cho bình phương tối thiểu không âm;
    \item minh họa hình phạt bậc hai trong Lagrangian tăng cường.
\end{itemize}

\subsection{Khớp đường cong bằng Gauss-Newton/LM}
\label{SUB_SEC_exp_gn_lm}
Xét dữ liệu sinh từ mô hình
\begin{equation}
    y_i=c_{\mathrm{true}}e^{a_{\mathrm{true}}t_i}+\varepsilon_i,
    \label{eq_exp_data_generation}
\end{equation}
trong đó $\varepsilon_i$ là nhiễu nhỏ. Nhiệm vụ là ước lượng $a$ và $c$ từ dữ liệu. Sai số dư được chọn là
\begin{equation}
    f_i(a,c)=y_i-ce^{at_i}.
    \label{eq_exp_residual_experiment}
\end{equation}
Jacobian theo hai tham số $a,c$ là
\begin{equation}
    \frac{\partial f_i}{\partial a}
    =
    -ct_i e^{at_i},
    \qquad
    \frac{\partial f_i}{\partial c}
    =
    -e^{at_i}.
    \label{eq_exp_jacobian}
\end{equation}
\medskip

Với Gauss-Newton, tại mỗi vòng lặp ta giải
\begin{equation}
    J(a_k,c_k)^{T}J(a_k,c_k)\delta_k
    =
    -J(a_k,c_k)^{T}F(a_k,c_k),
    \label{eq_exp_gn_system}
\end{equation}
rồi cập nhật
\begin{equation}
    \begin{bmatrix}
        a_{k+1}\\
        c_{k+1}
    \end{bmatrix}
    =
    \begin{bmatrix}
        a_k\\
        c_k
    \end{bmatrix}
    +\delta_k.
    \label{eq_exp_gn_update}
\end{equation}
Với Levenberg-Marquardt, hệ tuyến tính được thay bằng
\begin{equation}
    \left(J^{T}J+\lambda_k I\right)\delta_k
    =
    -J^{T}F.
    \label{eq_exp_lm_system}
\end{equation}
\medskip

Thực nghiệm được chạy với hai trường hợp khởi tạo:
\begin{itemize}
    \item khởi tạo gần nghiệm thật tại $(a_0,c_0)=(0.45,1.0)$;
    \item khởi tạo xa nghiệm thật tại $(a_0,c_0)=(-0.35,0.5)$.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/curve_fit_gn_lm_near.png}
    \caption{Kết quả khớp đường cong của Gauss--Newton và Levenberg--Marquardt từ khởi tạo gần.}
    \label{fig_exp_curve_fit_near}
\end{figure}

Hình~\ref{fig_exp_curve_fit_near} cho thấy từ khởi tạo gần, hai phương pháp hội tụ về cùng nghiệm $a\approx0.748$, $c\approx1.409$. Hai đường ước lượng gần như chồng khít với mô hình thật và bám sát dữ liệu nhiễu.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/curve_fit_gn_lm_far.png}
    \caption{Kết quả khớp đường cong của hai phương pháp từ cùng một khởi tạo xa.}
    \label{fig_exp_curve_fit_far}
\end{figure}

Khi dùng cùng khởi tạo xa, Hình~\ref{fig_exp_curve_fit_far} thể hiện sự khác biệt rõ rệt. Gauss--Newton đi tới nghiệm suy biến với $a\approx16.381$ và $c\approx8.46\times10^{-21}$, nên đường dự đoán gần bằng $0$ trên phần lớn miền rồi tăng đột ngột ở cuối. Ngược lại, cơ chế giảm chấn giúp Levenberg--Marquardt vẫn trở về nghiệm $a\approx0.748$, $c\approx1.41$ phù hợp với dữ liệu.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth]{image/experiments/convergence_gn_lm_near_far.png}
    \caption[Lịch sử hàm mục tiêu của GN và LM]{Lịch sử hàm mục tiêu của Gauss--Newton và Levenberg--Marquardt với hai cách khởi tạo.}
    \label{fig_exp_gn_lm_convergence}
\end{figure}

Trên Hình~\ref{fig_exp_gn_lm_convergence}, Gauss--Newton có thể làm hàm mục tiêu tăng mạnh ở các vòng đầu vì phiên bản thực nghiệm chấp nhận trực tiếp bước tuyến tính hóa. Hiện tượng này đặc biệt nghiêm trọng với khởi tạo xa. Levenberg--Marquardt chỉ chấp nhận bước làm giảm hàm mục tiêu, vì vậy đường hội tụ ổn định hơn và đạt cùng mức hàm mục tiêu cuối trong cả hai trường hợp.

\subsection{IRLS cho hồi quy \texorpdfstring{$L^1$}{L1}}
\label{SUB_SEC_exp_irls_l1}
Xét bài toán hồi quy tuyến tính
\begin{equation}
    b=Ax+\varepsilon,
    \label{eq_exp_l1_data}
\end{equation}
nhưng cố tình thêm một số ngoại lai vào vectơ $b$. Nếu dùng bình phương tối thiểu,
\begin{equation}
    \min_x \|Ax-b\|_2^2,
    \label{eq_exp_l2_regression}
\end{equation}
các ngoại lai có thể kéo nghiệm lệch mạnh. Thay vào đó, ta dùng hồi quy $L^1$:
\begin{equation}
    \min_x \|Ax-b\|_1.
    \label{eq_exp_l1_regression}
\end{equation}
IRLS cập nhật theo các bước:
\begin{equation}
    r^{(k)}=Ax_k-b,
    \qquad
    w_i^{(k)}=\frac{1}{\max(|r_i^{(k)}|,\delta)}.
    \label{eq_exp_l1_weights}
\end{equation}
Sau đó giải
\begin{equation}
    A^{T}W^{(k)}A x_{k+1}=A^{T}W^{(k)}b.
    \label{eq_exp_l1_weighted_system}
\end{equation}
\medskip

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/irls_l1_regression.png}
    \caption{So sánh bình phương tối thiểu và IRLS $L^1$ trên dữ liệu có ngoại lai.}
    \label{fig_exp_irls_regression}
\end{figure}

Trong Hình~\ref{fig_exp_irls_regression}, các điểm được khoanh đỏ là ngoại lai. Đường bình phương tối thiểu bị kéo lệch xuống vì bình phương các sai số dư lớn, trong khi đường IRLS $L^1$ gần trùng với đường sinh dữ liệu. Kết quả này minh họa trực tiếp khả năng giảm ảnh hưởng của ngoại lai khi dùng mất mát $L^1$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/convergence_irls_l1.png}
    \caption{Sự thay đổi của hàm mục tiêu $L^1$ trong quá trình lặp IRLS.}
    \label{fig_exp_irls_convergence}
\end{figure}

Hình~\ref{fig_exp_irls_convergence} cho thấy hàm mục tiêu giảm nhanh từ khoảng $66.1$ xuống dưới $62.7$ trong gần 10 vòng đầu, sau đó giảm chậm và ổn định quanh $62.55$. Phần đuôi hội tụ chậm phản ánh đặc trưng của IRLS khi các trọng số tiếp tục được hiệu chỉnh gần nghiệm.

\subsection{Quỹ đạo hạ theo tọa độ hai chiều}
\label{SUB_SEC_exp_coordinate_descent}
Để quan sát cơ chế cập nhật từng tọa độ, phương pháp hạ theo tọa độ được áp dụng cho một hàm bậc hai lồi hai biến. Tại mỗi bước chỉ $x_1$ hoặc $x_2$ được tối ưu, còn tọa độ kia được giữ cố định.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth]{image/experiments/coordinate_descent_zigzag.png}
    \caption{Quỹ đạo cập nhật luân phiên từng tọa độ trên các đường đồng mức.}
    \label{fig_exp_coordinate_descent}
\end{figure}

Các đoạn ngang và dọc trong Hình~\ref{fig_exp_coordinate_descent} tương ứng với việc chỉ thay đổi một tọa độ ở mỗi lần cập nhật. Do các đường đồng mức bị nghiêng và hai biến có tương quan, quỹ đạo có dạng zic-zắc. Dù vậy, độ dài bước giảm dần và dãy điểm tiến về nghiệm tối ưu được đánh dấu bằng ngôi sao.

\subsection{K-means và ADMM cho bình phương tối thiểu không âm}
\label{SUB_SEC_exp_kmeans_admm}
\textbf{K-means.} Dữ liệu hai chiều gồm $K$ cụm được sinh mô phỏng, sau đó k-means được chạy từ một bộ tâm ban đầu cố định. Hàm mục tiêu là
\begin{equation}
    E=\sum_{i=1}^{m}\|x_i-y_{c_i}\|_2^2.
    \label{eq_exp_kmeans_energy}
\end{equation}
Sau mỗi vòng lặp, giá trị $E$ được ghi lại để kiểm tra tính giảm của thuật toán.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth]{image/experiments/kmeans_clusters.png}
    \caption{Kết quả phân cụm k-means trên dữ liệu mô phỏng hai chiều.}
    \label{fig_exp_kmeans_clusters}
\end{figure}

Hình~\ref{fig_exp_kmeans_clusters} cho thấy ba nhóm được tách rõ, còn các dấu nhân màu đen nằm gần trung tâm của từng đám mây điểm. Đây là kết quả của việc luân phiên giữa bước gán nhãn theo tâm gần nhất và bước lấy trung bình để cập nhật tâm.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/kmeans_objective.png}
    \caption{Objective của k-means qua các vòng lặp.}
    \label{fig_exp_kmeans_objective}
\end{figure}

Trong Hình~\ref{fig_exp_kmeans_objective}, hàm mục tiêu giảm từ khoảng $1560$ xuống gần $125$ sau ba vòng cập nhật và giữ nguyên ở vòng tiếp theo. Kết quả phù hợp với tính chất mỗi bước tối ưu luân phiên không làm tăng hàm mục tiêu. Tuy nhiên, thí nghiệm này dùng một bộ tâm ban đầu cố định, nên không loại bỏ được khả năng hội tụ tới nghiệm khác khi thay đổi khởi tạo.
\medskip

\textbf{ADMM cho bình phương tối thiểu không âm.} Bài toán được xét là
\begin{equation}
    \min_x \|Ax-b\|_2^2
    \quad
    \text{với điều kiện}
    \quad
    x\geq 0.
    \label{eq_exp_nnls}
\end{equation}
Với tách biến $x=z$, các bước ADMM là
\begin{equation}
    x_{k+1}
    =
    (2A^{T}A+\rho I)^{-1}(2A^{T}b+\rho z_k-\lambda_k),
    \label{eq_exp_nnls_x}
\end{equation}
\begin{equation}
    z_{k+1}
    =
    \max\left(x_{k+1}+\frac{1}{\rho}\lambda_k,0\right),
    \label{eq_exp_nnls_z}
\end{equation}
\begin{equation}
    \lambda_{k+1}
    =
    \lambda_k+\rho(x_{k+1}-z_{k+1}).
    \label{eq_exp_nnls_lambda}
\end{equation}
Hai đại lượng được ghi lại là phần dư nguyên thủy
\begin{equation}
    r_{\mathrm{pri}}^{(k)}=\|x_k-z_k\|_2
    \label{eq_exp_primal_residual}
\end{equation}
và giá trị hàm mục tiêu $\|Ax_k-b\|_2^2$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/admm_nnls_convergence.png}
    \caption{Hàm mục tiêu, phần dư nguyên thủy và phần dư đối ngẫu của ADMM cho NNLS.}
    \label{fig_exp_admm_convergence}
\end{figure}

Hình~\ref{fig_exp_admm_convergence} cho thấy phần dư đối ngẫu giảm rất nhanh trong vài vòng đầu rồi tiếp tục giảm tới cỡ $10^{-5}$. Phần dư nguyên thủy giảm chậm hơn, từ cỡ $2\times10^{-2}$ xuống dưới $10^{-2}$. Hàm mục tiêu sớm ổn định quanh $1.5\times10^{-1}$ và có thể dao động nhẹ vì biến $x_k$ chưa thỏa chính xác ràng buộc $x_k=z_k$ ở các vòng trung gian; ADMM không yêu cầu hàm mục tiêu phải giảm đơn điệu ở mọi vòng.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth]{image/experiments/admm_nnls_solution.png}
    \caption{So sánh nghiệm không âm do ADMM ước lượng với vectơ hệ số thật.}
    \label{fig_exp_admm_solution}
\end{figure}

Trên Hình~\ref{fig_exp_admm_solution}, bảy hệ số khác $0$ được khôi phục gần đúng cả về vị trí lẫn độ lớn, còn các hệ số còn lại nằm sát $0$ và không âm. Vì dữ liệu có nhiễu, nghiệm ước lượng không trùng tuyệt đối với vectơ thật, nhưng vẫn bảo toàn tốt cấu trúc thưa của tín hiệu.

\subsection{Ảnh hưởng của hệ số phạt trong Lagrangian tăng cường}
\label{SUB_SEC_exp_augmented_lagrangian}
Xét hàm mục tiêu minh họa $f(x,y)=xy$ với ràng buộc $x+y=1$. Thành phần phạt bậc hai là
\begin{equation}
    \frac{\rho}{2}(x+y-1)^2.
    \label{eq_exp_quadratic_penalty}
\end{equation}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth]{image/experiments/augmented_lagrangian_penalty.png}
    \caption{Đường đồng mức của hàm mục tiêu có phạt khi tăng $\rho$; đường đỏ đứt nét là tập ràng buộc $x+y=1$.}
    \label{fig_exp_augmented_lagrangian}
\end{figure}

Khi $\rho=0$, hình dạng đường đồng mức chỉ do $xy$ quyết định. Khi $\rho$ tăng từ $0.1$ lên $10$, độ lệch khỏi đường $x+y=1$ bị phạt mạnh hơn và các đường đồng mức dần chịu chi phối bởi khoảng cách tới tập ràng buộc. Hình~\ref{fig_exp_augmented_lagrangian} vì vậy minh họa vai trò của hình phạt bậc hai: ưu tiên các điểm gần thỏa ràng buộc, nhưng không khẳng định rằng chỉ cần một giá trị $\rho$ hữu hạn là đã thỏa ràng buộc chính xác.

\subsection{So sánh tốc độ hội tụ}
\label{SUB_SEC_exp_compare}
Các thí nghiệm trên có thể được so sánh theo ba tiêu chí:
\begin{itemize}
    \item \textbf{Số vòng lặp}: thuật toán cần bao nhiêu vòng để đạt điều kiện dừng.
    \item \textbf{Chi phí mỗi vòng}: mỗi vòng cần giải hệ tuyến tính, chiếu, cập nhật trọng số hay chỉ tính trung bình.
    \item \textbf{Độ ổn định}: thuật toán có nhạy với khởi tạo, ngoại lai hoặc tham số như $\lambda$, $\rho$, $\delta$ hay không.
\end{itemize}
\medskip

Các hình thực nghiệm xác nhận sự đánh đổi giữa các phương pháp. Gauss--Newton đạt nghiệm tốt khi khởi tạo gần nhưng thất bại trong trường hợp khởi tạo xa đã xét, còn Levenberg--Marquardt ổn định ở cả hai trường hợp. IRLS tạo đường hồi quy ít bị ngoại lai kéo lệch nhưng hội tụ chậm ở phần đuôi. Hạ theo tọa độ tiến theo quỹ đạo zic-zắc do cập nhật từng biến. K-means giảm hàm mục tiêu rất nhanh trên dữ liệu có ba cụm tách biệt. Với NNLS, ADMM khôi phục tốt nghiệm không âm dù phần dư ràng buộc cần nhiều vòng để tiếp tục giảm.
\medskip

Các kết quả thực nghiệm làm rõ những đánh đổi khó thấy nếu chỉ nhìn công thức: tốc độ hội tụ, độ ổn định, chi phí mỗi vòng lặp và độ nhạy với tham số.
